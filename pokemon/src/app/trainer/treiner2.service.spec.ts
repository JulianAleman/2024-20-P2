/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { Treiner2Service } from './treiner2.service';

describe('Service: Treiner2', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [Treiner2Service]
    });
  });

  it('should ...', inject([Treiner2Service], (service: Treiner2Service) => {
    expect(service).toBeTruthy();
  }));
});
